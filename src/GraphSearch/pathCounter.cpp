#include "pathCounter.h"

PathFinder::PathFinder(StringGraph* pGraph, int maxGap, int maxOL) :
pGraph_(pGraph),
maxGap_(maxGap),
maxOL_(maxOL)
{


}

void PathFinder::initCountMap()
{

    // For each edge in the graph, add an entry to the count map



}
