//

class ClosureOverlap
{

    public:
    ClosureOverlap(const Closure * c1, size_t s1, size_t e2,
                    const Closure * c2, size_t s2, size_t e2,
                    bool isRC);

    private:
    const Closure * c1_;
    const Closure * c2_;
    size_t s1_;
    size_t e1_;
    size_t s2_;
    size_t e2_;
    bool isRc_;
};

clas ClosureOverlapper
{

    public:
    ClosureOverlapper(const 




    private:

}
